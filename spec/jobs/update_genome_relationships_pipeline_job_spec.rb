require 'spec_helper'

describe UpdateGenomeRelationshipsPipelineJob do

  it 'builds the genome relationships pipeline from scratch'

  it 'can update the genome relationships graph with the addition of a new genome'

  it 'the updated version of the graph is equivalent to the graph built form scratch'

  it 'only creates *new* feature relationships'

  it 'only creates *new* genome relationships'

end
